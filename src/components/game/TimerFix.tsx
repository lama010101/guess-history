
import { useEffect } from 'react';
import './timer-fix.css';

export const TimerFix = () => {
  // This component just imports the CSS and doesn't render anything
  return null;
};

export default TimerFix;
